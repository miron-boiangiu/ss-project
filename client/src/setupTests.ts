// Vitest global setup. Loaded once before any test file runs.
// Registers @testing-library/jest-dom's matchers (toBeInTheDocument,
// toHaveTextContent, etc.) on Vitest's expect.
import '@testing-library/jest-dom/vitest';
